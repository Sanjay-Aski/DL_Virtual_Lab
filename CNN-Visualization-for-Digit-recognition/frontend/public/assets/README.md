Place the provided VES logo image file in this folder with the exact name `ves_logo.png`.

The React app references the logo at `/assets/ves_logo.png` (i.e. put the file at
`public/assets/ves_logo.png`).

If you prefer a different filename, update the `src` in
`frontend/src/App.js` where `process.env.PUBLIC_URL` is used.

Provided logo (from project attachments) should be saved as:

- `public/assets/ves_logo.png`
